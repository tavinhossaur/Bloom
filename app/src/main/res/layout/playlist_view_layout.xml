<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="180dp"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp" android:layout_marginVertical="10dp"
    android:clickable="true"
    app:cardElevation="8dp"
    app:cardCornerRadius="0dp"
    app:cardPreventCornerOverlap="false"
    tools:context=".PlaylistsActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:orientation="vertical">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Capa da playlist -->
            <ImageView
                android:id="@+id/img_playlist_placeholder"
                android:layout_width="match_parent"
                android:layout_height="180dp"
                android:background="@android:color/transparent"
                android:scaleType="centerCrop" />

            <!-- Capa da playlist -->
            <ImageView
                android:id="@+id/img_playlist1"
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:background="@android:color/transparent"
                android:scaleType="centerCrop" />

            <!-- Capa da playlist -->
            <ImageView
                android:id="@+id/img_playlist2"
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:layout_toEndOf="@id/img_playlist1"
                android:background="@android:color/transparent"
                android:scaleType="centerCrop" />

            <!-- Capa da playlist -->
            <ImageView
                android:id="@+id/img_playlist3"
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:layout_below="@id/img_playlist1"
                android:background="@android:color/transparent"
                android:scaleType="centerCrop" />

            <!-- Capa da playlist -->
            <ImageView
                android:id="@+id/img_playlist4"
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:layout_below="@id/img_playlist2"
                android:layout_toEndOf="@id/img_playlist3"
                android:background="@android:color/transparent"
                android:scaleType="centerCrop"/>

        </RelativeLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="13dp">

            <!-- Nome da playlist -->
            <TextView
                android:id="@+id/nome_playlist"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Nome da playlist"
                android:textColor="@color/purple1"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"
                android:textAppearance="?attr/textAppearanceBody1" />

            <!-- Nome do criador playlist -->
            <TextView
                android:id="@+id/criador_playlist"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Criado por: nome"
                android:maxLines="1"
                android:ellipsize="end"
                android:textAppearance="?attr/textAppearanceBody2"
                android:textColor="@color/grey3"/>

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>